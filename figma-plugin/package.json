{
  "name": "cursor-figma-assistant",
  "version": "1.0.0",
  "description": "Figma plugin for Cursor integration",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.js",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "typescript": "^5.3.2",
    "esbuild": "^0.19.11"
  }
} 